<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Theo Moss</title>
<link rel="stylesheet" href="/css/style.css">
</head>
<body>

<!-- Hero Section -->
<header class="hero">
  <h1>Theo Moss</h1>
</header>

<!-- Portfolio Section -->
<section class="portfolio-section">
  <div class="portfolio-grid">
    <% projects.forEach(p => { %>
      <div class="project">
        <div class="project-images images-<%= p.images.length %>">
          <% p.images.forEach(img => { %>
            <img 
              src="/uploads/<%= img %>" 
              alt="<%= p.name %>" 
              onclick="openModal('/uploads/<%= img %>', '<%= p.name %>')"
            >
          <% }) %>
        </div>
        <div class="overlay">
          <h3><%= p.name %></h3>
          <p><%= p.description %></p>
        </div>
      </div>
    <% }) %>
  </div>
</section>

<!-- About Me Section -->
<section class="about-section">
  <h2>About Me</h2>
  <p>Hello, I'm Theo and I've been making textures on Adobe 3D Substance Painter since December 2024. I also create texturing tools like DTR on my GitHub and maps for games, specifically Descenders on Unity 2017.</p>
</section>

<!-- Contact Section -->
<section class="contact-section">
  <h2>Contact Me</h2>
  <p>You can reach out to me at: <a href="mailto:theojudemoss@gmail.com">theojudemoss@gmail.com</a></p>
  <p>Check out my GitHub: <a href="https://github.com/THEE-OH" target="_blank">https://github.com/THEE-OH</a></p>
</section>

<!-- Modal -->
<div id="modal" class="modal" onclick="closeModal(event)">
  <span class="close">&times;</span>
  <img id="modal-img" class="modal-img">
  <div id="caption"></div>
</div>

<script src="/js/script.js"></script>
</body>
</html>
